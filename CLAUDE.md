# CLAUDE.md

このリポジトリは「通勤サウンドスケープ」Androidアプリ（アプリ名 ekifu）です。正式な指示書は `doc/SPEC.md` で、本ファイルはその要点です。迷ったら必ず SPEC.md を読み直してください。

## コンセプト

- **場所が楽譜になる**：約1km四方のマス（ジオハッシュ6桁）ごとに固有のモチーフがあり、同じ場所では同じメロディが鳴る
- 経路の登録は不要。約5分ごとの位置情報を「場面」の区切りにして、癒し系インストゥルメンタルを端末上で自動生成・再生する
- 登録地点（自宅・職場・公園など、最大20件）に近づくと地点のテーマが聞こえ、着くと曲が「着地」する
- 生成はAIではなく**ルールベース合成**（和音・音階・リズムをプログラムで組み合わせる）。オフライン動作、API費用なし
- Web試作（Web Audio API）の音作りの値を初期値として移植する

## MVPのスコープ

- 作る：位置情報からの音楽生成、地点登録と接近時の変化、画面オフでも途切れない再生（フォアグラウンドサービス）、ロック画面・通知からの操作、再生画面、地点の一覧・登録画面
- 作らない：経路登録、駅単位の演出、地上/地下判定、iOS版、アカウント、クラウド同期、移動記録の保存、共有、歩行リズム等の追加入力

## 技術方針

| 項目 | 採用 |
| --- | --- |
| 言語・UI | Kotlin、Jetpack Compose |
| 最小SDK | API 26（Android 8.0） |
| 音声出力 | `AudioTrack` ストリーミング、PCM 16bit、44.1kHz、ステレオ |
| 音声合成 | Kotlin自前実装（オシレーター、エンベロープ、フィルター、ディレイ、リバーブ、コンプレッサー）。外部依存なし |
| バックグラウンド | フォアグラウンドサービス（種別 mediaPlayback + location） |
| メディア操作 | Media3 `MediaSession` |
| 位置情報 | Fused Location Provider、`PRIORITY_BALANCED_POWER_ACCURACY`、通常5分間隔／登録地点1.5km以内は1分 |
| 保存 | Room（登録地点、訪問履歴＝マスIDと訪問日のみ） |

- 合成はUIスレッドから切り離す（専用スレッド or `Dispatchers.Default`）
- 乱数はシード付き（Mulberry32）。同じシード → 同じ結果
- オーディオフォーカス取得、着信・他アプリ再生・イヤホン抜け（`ACTION_AUDIO_BECOMING_NOISY`）で一時停止
- 音は10秒チャンクで先回り生成し、30〜60秒ぶんをバッファ。シンセ状態はチャンクをまたいで引き継ぐ。作曲（音符イベント）と合成（波形）を分離する
- 位置の正確な緯度経度・軌跡は保存しない。外部送信しない
- GitHub Actions でデバッグ用APKをビルドし、成果物（artifact）としてダウンロードできるようにする

## 実装ステップ（SPEC.md 10章）

| 段階 | 作るもの | 完了条件 |
| --- | --- | --- |
| 1. 音の再現 | 合成エンジン（4章）、デモ再生、APKビルド用GitHub Actions。位置なし・画面オンのみ | デモ再生が途切れず鳴り、始まり・道中・接近・到着・滞在・終わりの違いが聞き分けられる |
| 2. バックグラウンド | チャンク生成とバッファ（7章）、フォアグラウンドサービス、MediaSession | 画面オフで30分間音切れなし。ロック画面から一時停止・再開できる |
| 3. おまかせ再生 | 5分ごとの位置取得、場面決定、ジオハッシュ、訪問履歴、太陽高度 | 30分以上移動して5分ごとに場面が音切れなく切り替わり、同じ場所で同じモチーフが鳴る |
| 4. 登録地点 | 地点の一覧・登録画面と保存、取得間隔の切替、接近・到着・離脱の判定と演出 | 外側の円に入って2分以内に接近演出、内側の円で到着演出 |
| 5. 仕上げ | 画面デザイン、オーディオフォーカス、イヤホン抜け、設定画面 | 11章の受け入れ基準をすべて満たす |

## 進め方のルール

- **段階ごとに区切る**。1段階終えたら「何を作ったか」と「実機での確認手順」を報告して止まる。次の段階へ勝手に進まない
- 各段階の終わりに、実機（Android）での確認手順を `README.md` に追記する
- 音のパラメータは `MusicConstants.kt` などに集約し、コードに数値を直書きしない
- 作曲ロジック（音符イベントを返す純粋関数）、場面の決定、接近の判定には単体テストを書く
- **仕様に曖昧な点があれば推測で進めず、先に質問する**
- 開発用にデバッグ表示（場面・フェーズ・最寄り地点までの距離・バッファ残量）と「デモ再生」（5分の場面を30秒に短縮、自宅→公園→駅→職場の架空移動）を用意する

## ユーザーと決めたこと（SPEC.md にない、または SPEC.md より優先）

- アプリ名は「ekifu」。ホーム画面・再生画面の見出し・通知とロック画面のタイトルすべて（SPEC 9章の「通勤のサウンドスケープ」を置き換える）。パッケージ名は `jp.fmt.ekifu`
- Web試作のコードは使わず、仕様書の値だけで作る。仕様書にない値は `MusicConstants.kt` に「仕様外・要調整」と書く
- デモ再生：5分の場面は30秒、区切りは道中2分ごとに縮めるが、始まりは1分のまま。場面の変化（速さ・なじみ度・太陽）と雰囲気もデモ台本で鳴らす
- 状態の変化の反映（7章）：30〜60秒の作り置きは保つが、変化があれば再生位置から1秒以上先のチャンクを捨て、その頭で保存したエンジンの状態から作り直す（遅れは最大約11秒）
- 段階2で画面オフ再生するのはデモ再生。フォアグラウンドサービスの location 種別は位置を使う段階3で追加する
