# 通勤サウンドスケープ アプリ制作指示書（Claude Code向け）

## 1. 概要とコンセプト

通勤中の移動そのものを楽譜にして、癒し系インストゥルメンタルを端末上で自動生成・再生するAndroidアプリを作る。

- **コンセプト**：「ルートが楽譜になる」。駅ごとに固有のメロディがあり、出発から到着までで曲に起承転結がつく
- **Endel / Brain.fm との違い**：両者は「今の状態（時間・天気・心拍、集中・睡眠）」に合わせる音。本アプリは「移動の旅程」に合わせる音で、毎日同じルートなら自分の通勤のテーマ曲になる
- **前提**：Web試作（Web Audio API、ルールベース生成）で音の方向性とバックグラウンド再生の方式を検証済み。その仕様を本指示書の4章以降に移植する
- **生成方式**：AIモデルではなく、和音・音階・リズムをプログラムで組み合わせるルールベース合成。オフラインで動き、API費用がかからない

## 2. MVPのスコープ

MVPは「登録した1本の通勤ルートで、画面オフでも途切れず、位置に合わせて曲が進む」ことだけを満たす。

| 区分 | 内容 |
| --- | --- |
| 作る | 通勤ルート1本の登録（駅リスト、座標、地下区間フラグ、所要時間の目安） |
| 作る | 位置・経過時間・地上/地下の3要素で変化する音楽生成エンジン |
| 作る | 画面オフでも途切れないバックグラウンド再生（フォアグラウンドサービス） |
| 作る | ロック画面・通知からの再生/一時停止 |
| 作る | 路線図風の再生画面（現在地、経過/残り時間、フェーズ表示） |
| 作らない | iOS版、複数ルート、移動の自動検出、アカウント、クラウド同期 |
| 作らない | 音の日記（移動記録の保存）、他ユーザーとの共有 |
| 作らない | 速度・歩行リズム・明るさ・騒音などの追加入力（将来の拡張） |

## 3. 技術方針

Androidネイティブ（Kotlin）で作り、音声は自前で合成したPCMを `AudioTrack` に隙間なく流し込む。Web試作で、Chromeの画面オフ時にリアルタイム生成が途切れることを確認したため、OSに正式に守られる再生経路を使う。

| 項目 | 採用するもの | 理由 |
| --- | --- | --- |
| 言語・UI | Kotlin、Jetpack Compose | 標準構成で扱いやすい |
| 最小SDK | API 26（Android 8.0）以上 | フォアグラウンドサービスとAudioTrackの安定動作 |
| 音声出力 | `AudioTrack`（ストリーミングモード、PCM 16bit、44.1kHz、ステレオ） | 生成したバッファを隙間なく連結できる |
| 音声合成 | Kotlinで自前実装（オシレーター、エンベロープ、フィルター、ディレイ、リバーブ） | 外部依存なし、試作の音を再現しやすい |
| バックグラウンド | フォアグラウンドサービス（`foregroundServiceType="mediaPlayback|location"`） | 画面オフ・他アプリ使用中も継続 |
| メディア操作 | Media3 の `MediaSession` | ロック画面・通知・Bluetoothイヤホンのボタン対応 |
| 位置情報 | Fused Location Provider（`PRIORITY_BALANCED_POWER_ACCURACY`） | 省電力で駅単位の精度は十分 |
| 保存 | Room または DataStore（ルート情報） | ルート1本なのでシンプルに |

- 音声合成はUIスレッドから切り離し、専用スレッド（またはコルーチンの `Dispatchers.Default`）で行う
- オーディオフォーカスを取得し、電話着信・他アプリ再生時は一時停止する
- イヤホンが抜けたら（`ACTION_AUDIO_BECOMING_NOISY`）一時停止する
- GitHub Actions でデバッグ用APKをビルドし、成果物としてダウンロードできるようにする（スマホで直接インストールして試すため）

## 4. 音楽生成エンジンの仕様

Web試作の値をそのまま初期値として移植する。すべての値は定数ファイルにまとめ、あとで調整できるようにする。

### 基本設定

- 調：Fメジャー（ルート音 MIDI 53 = F3）
- 1拍 = 0.9秒（約67BPM）、和音は8拍ごとに切り替え
- メロディ音域：MIDI 65〜86 のFメジャー・ペンタトニック（F, G, A, C, D）
- 乱数はシード付き（例：Mulberry32）にし、同じシードなら同じ曲になるようにする

### 和音（ルートからの半音数）

| 名前 | 構成音 |
| --- | --- |
| I | 0, 4, 7, 11, 14 |
| IV | 5, 9, 12, 16, 19 |
| vi | 9, 12, 16, 19 |
| ii | 2, 5, 9, 12, 16 |
| iii | 4, 7, 11, 14 |
| Vsus | 7, 12, 14, 17 |

### フェーズ（旅程の進み具合 p = 経過 ÷ 予定所要時間）

| フェーズ | 範囲 | 和音進行 | 1拍あたりのメロディ発音確率 |
| --- | --- | --- | --- |
| 出発 | p < 0.2 | I → IV | 0.18 |
| 道中 | 0.2 ≤ p < 0.75 | vi → IV → I → Vsus → ii → iii → IV → I | 0.55 |
| 到着前 | 0.75 ≤ p < 1 | IV → Vsus → IV → I | 0.38 |
| 到着 | p ≥ 1 | I を長く鳴らし、約12秒でフェードアウト | なし |

フェーズが変わったら、次の和音の切り替え時に新しい進行の先頭から始める。発音は拍の頭と裏（0.5拍）で判定し、裏は確率を0.6倍にする。

### 音色

| パート | 作り方 | 役割 |
| --- | --- | --- |
| パッド | 三角波2本（±6セント）→ ローパス1400Hz、立ち上がり2.5秒、余韻3.5秒 | 和音の土台 |
| ベース | サイン波、和音の根音を1オクターブ下で、立ち上がり3秒 | 低音の支え |
| プラック | 三角波、2.2秒で指数減衰、左右にランダム定位 | メロディ |
| ベル | サイン波の倍音（×1、×2.76、×5.4）、減衰4秒 | 駅のメロディ |

- 全体にディレイ（0.675秒、フィードバック0.32、ローパス2200Hz）とリバーブ（残響3.5秒程度）をかける
- メロディは直前の音から7半音以内で、現在の和音の構成音を優先して選ぶ（和音外の音は35%の確率で許可）
- 最後にコンプレッサーで音量をならす

### 駅のメロディ

駅ごとに3〜4音のモチーフを持つ。MVPでは登録時に駅名から自動生成し、ペンタトニック内の音から選ぶ。

- 駅を通過した瞬間に、ベルで0.5拍間隔で鳴らす
- 道中フェーズでは24拍ごとに、直前に通った駅のモチーフをプラックで静かに再現する
- 終点のモチーフは主音（F）で終わるようにする

## 5. 入力要素とルートデータ

入力は「位置」「経過時間」「地上/地下」の3つに絞る。地上/地下はセンサーで推測せず、ルート登録時の区間フラグで決める。

### ルートデータ

```json
{
  "name": "いつもの通勤",
  "expectedMinutes": 18,
  "stations": [
    { "name": "若葉台", "lat": 35.000, "lng": 139.000, "undergroundAfter": false },
    { "name": "川辺",   "lat": 35.010, "lng": 139.020, "undergroundAfter": true }
  ]
}
```

- `undergroundAfter`：その駅から次の駅までが地下区間かどうか
- MVPでは、アプリ内のフォームで駅名・座標（地図タップ）・地下フラグを手入力する。駅データAPIとの連携は後回し

### 各要素の算出と音への反映

| 要素 | 算出方法 | 音への反映 |
| --- | --- | --- |
| 位置 | 現在地をルート上の最寄り区間に射影し、駅から半径150m以内に入ったら「通過」とする | 駅のメロディを鳴らす、直前の駅を記録 |
| 経過時間 | 再生開始からの実時間 | 進み具合 p の計算に使う |
| 残り時間 | 残り距離 ÷ 直近の平均速度。速度が取れないときは予定所要時間から逆算 | 到着前フェーズの開始タイミング |
| 地上/地下 | 現在の区間の `undergroundAfter` | 地下：全体ローパスを6000Hz→850Hz、リバーブ量0.35→0.7、和音とメロディを1オクターブ下げ、発音確率を0.7倍（時定数2.5秒でなめらかに変化） |

- 進み具合 p は「経過 ÷（経過 + 推定残り時間）」で計算し、遅延すると p の伸びがゆっくりになるようにする
- 地下で位置が取れない間は、最後の位置と予定所要時間から推定して曲を進める
- 位置情報の許可がない場合は、経過時間だけで進むモードで動かす

## 6. チャンク生成とバッファ設計

音は10秒単位のチャンクで先回りして作り、常に30〜60秒ぶんを再生待ちに置く。これで再生開始は1秒以内、画面オフでも途切れず、位置の変化にも1チャンク遅れで追従できる。

処理の流れ：位置・時間の入力 → 状態の更新（p・フェーズ・地下）→ 作曲（拍ごとの音符イベント）→ 合成（10秒のPCMチャンク）→ バッファ（30〜60秒）→ AudioTrack

作曲（音符を決める）と合成（波形を作る）を分けておくと、先読み中に状態が変わっても未合成の部分だけ作り直せる。

- **つなぎ目**：シンセの状態（鳴っている音の残り、ディレイ・リバーブの内部バッファ、フィルターの状態）をチャンクをまたいで引き継ぐ。チャンクごとに新しく作り直さない
- **開始**：最初のチャンクは2秒ぶんだけ先に合成してすぐ再生し、残りは再生しながら埋める
- **状態の変化**：駅通過や地下への出入りは、次に合成するチャンクから反映する（最大10秒の遅れは許容）
- **大きな遅延**：推定残り時間が大きく変わったら、未合成の作曲イベントを破棄して作り直す
- **バッファ不足**：生成が追いつかなかった場合は無音を挟まず、直前のパッドを伸ばしてつなぐ
- **メモリ**：44.1kHzステレオ16bitで60秒 ≒ 10.6MB。長時間の移動でも常にこの上限に収める

## 7. バッテリーと位置情報の方針

目標は「普段の音楽アプリの再生と同程度の消費」。一番効くのは位置情報の取得頻度なので、駅単位で足りる精度に抑える。

- 位置の取得間隔は30秒、最小移動距離100mで更新（`setMinUpdateDistanceMeters`）
- 精度は `PRIORITY_BALANCED_POWER_ACCURACY`。高精度GPSは使わない
- 駅の半径500m以内に近づいたときだけ、間隔を10秒に短縮して通過判定の精度を上げる
- 合成はバッファが30秒を切ったときにまとめて行い、それ以外はスレッドを休ませる
- 画面オフ中はUIの更新（地図の描画）を止める
- 一時停止中は位置の取得と合成を両方止める
- 到着判定後は自動で位置の取得を止め、フォアグラウンドサービスを終了する

### 必要な権限

- `ACCESS_FINE_LOCATION` と `ACCESS_COARSE_LOCATION`（アプリ使用中の許可で足りる。サービスはユーザー操作から起動するため）
- `FOREGROUND_SERVICE`、`FOREGROUND_SERVICE_MEDIA_PLAYBACK`、`FOREGROUND_SERVICE_LOCATION`
- `POST_NOTIFICATIONS`（Android 13以上、再生中の通知のため）

## 8. 画面仕様

画面は「再生」「ルート登録」の2つだけ。見た目は路線図風のデザインにする。

| 画面 | 表示・操作 |
| --- | --- |
| 再生 | 縦の路線図（駅、地下区間の帯、現在地の点）、経過時間と到着までの時間、現在のフェーズ名と一言説明、地上/地下の表示、直前に鳴った駅名、再生/一時停止ボタン、最初からボタン |
| ルート登録 | 駅の追加・並べ替え・削除、地図タップで座標指定、区間ごとの地下フラグ、予定所要時間 |
| 通知・ロック画面 | タイトル「通勤のサウンドスケープ」、サブタイトル「出発駅 → 到着駅」、再生/一時停止 |

- 配色の初期値：背景 #E4E9E7、文字 #25323B、路線 #6F9A8D、地下の帯 #CCD6D8、現在地 #D39A2E。ダークモードでは背景 #1B2328、文字 #E2E8E5、現在地 #E3B45A
- フォントは丸ゴシック系（Zen Maru Gothic を同梱、なければ端末標準）
- ルート未登録時の再生画面には「通勤ルートを登録する」ボタンだけを出す
- 開発用に「デモ再生」（18分のルートを3分に縮めて再生、位置は擬似的に進める）を残す。駅は 若葉台(0分)・桜坂(3分)・川辺(6分)・中央(9分)・港町(12分)・丘の上(15分)・汐見(18分)、地下区間は全体の27%〜72%

## 9. 実装ステップ

5段階で進め、各段階の完了条件を満たしてから次へ進む。各段階の終わりに実機（Android）での確認手順を README に追記すること。

| 段階 | 作るもの | 完了条件 |
| --- | --- | --- |
| 1. 音の再現 | 合成エンジン（4章）とデモ再生、APKビルド用のGitHub Actions。位置なし、画面オンのみ | デモ再生で3分の曲が最後まで途切れず鳴り、パッド・プラック・ベル・地下での変化が聞き取れる |
| 2. バックグラウンド | チャンク生成とバッファ（6章）、フォアグラウンドサービス、MediaSession | 画面オフのまま30分間、音切れなしで再生できる。ロック画面から一時停止・再開できる |
| 3. ルート登録 | ルート登録画面と保存（5章のデータ形式） | 駅5つ以上のルートを登録・編集でき、アプリ再起動後も残る |
| 4. 位置連動 | 位置取得（7章）、通過判定、残り時間推定、地上/地下の切り替え | 実際の移動で、駅通過から10秒以内にその駅のメロディが鳴る。位置の許可がなくても経過時間モードで動く |
| 5. 仕上げ | 再生画面のデザイン、オーディオフォーカス、イヤホン抜け対応、到着時の自動終了 | 10章の受け入れ基準をすべて満たす |

### 進め方の指示

- 段階ごとに作業を区切り、終わったら何を作ったかと実機での確認手順を報告して止まること
- 音のパラメータは `MusicConstants.kt` などに集約し、コードに直接数値を書かない
- 作曲ロジック（音符イベントを返す純粋な関数）には単体テストを書く。同じシードで同じ結果になること、フェーズごとの和音進行が仕様どおりであることを確認する
- 仕様に曖昧な点があれば、推測で進めず質問すること

## 10. 受け入れ基準とテスト方法

MVPの完成は、以下をすべて実機で確認できた時点とする。

| 項目 | 基準 | 確かめ方 |
| --- | --- | --- |
| 再生開始 | ボタンを押してから1秒以内に音が出る | 実機で10回計測 |
| 画面オフ | 60分間、音切れ・ノイズなし | 画面オフで放置し、イヤホンで聴く。ログでバッファ不足の回数が0であること |
| バッテリー | 60分の再生で、普段使う音楽アプリの1.5倍以内 | 「設定 → 電池」のアプリ別使用量で比較 |
| 駅のメロディ | 駅通過から10秒以内に鳴る | 実際の通勤で全駅を確認 |
| 地下区間 | 地下に入ると音がこもり、出ると戻る。変化は急に切り替わらない | 地下鉄区間で確認 |
| 遅延への追従 | 予定より5分遅れても、到着の手前で到着前フェーズに入る | デモ再生で擬似的に遅延させて確認 |
| 割り込み | 電話着信・他アプリ再生・イヤホン抜けで一時停止し、再開できる | 各操作を実機で確認 |
| 到着 | 最後の和音で終わり、位置取得とサービスが止まる | 到着後に通知が消えていること |

開発中は、デバッグ用に現在の p・フェーズ・地下フラグ・バッファ残量を画面に出すオプションを用意すること。
